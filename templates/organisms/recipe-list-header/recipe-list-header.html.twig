<section class="recipe-list-header">
    <div class="background blue">
        <div class="container">
            <div class="row">
                <div class="col s12 offset-m1 m10 offset-l1 l10 offset-xl2 xl8">
                    <div class="filter-container">
                        <div class="flex-fill"></div>
                        {{ form_start(form) }}
                            {{ include('molecules/searchbar/searchbar.html.twig', {
                                form: form
                            }) }}
                        {{ form_end(form) }}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>